```bash
# .env.example
# Wiki.js connection
WIKIJS_BASE_URL=https://wiki.example.com
WIKIJS_API_TOKEN=changeme
WIKIJS_LOCALE=en

# Optional API key to protect internal wikimgr endpoints.
# If unset, the API key check is disabled (dev convenience).
WIKIMGR_API_KEY=dev-optional

# Canonical roots for path preflight validation.
# Comma-separated; defaults in code to: homelab,projects,ai,personal,community,meta
WIKIMGR_ALLOWED_ROOTS=homelab,projects,ai,personal,community,meta

# Internal service discovery for bulk ops. Prefer setting a single base URL
# (e.g. docker-compose service host) and let individual endpoints default
# from it. You can still override specific endpoints with *_URL variables.
# Default base in the code is http://127.0.0.1:8080
WKMGR_BASE_URL=http://127.0.0.1:8080
# Optional per-endpoint overrides (if you host one of these elsewhere)
# WKMGR_UPSERT_URL=${WKMGR_BASE_URL}/pages/upsert
# WKMGR_GET_URL=${WKMGR_BASE_URL}/wikimgr/get
# WKMGR_DELETE_URL=${WKMGR_BASE_URL}/wikimgr/delete
# WKMGR_INVENTORY_JSON=${WKMGR_BASE_URL}/wikimgr/pages/inventory.json

# Optional bearer for internal calls between components
# WKMGR_BEARER=
```
